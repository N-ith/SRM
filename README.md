# Secure Remove [SRM]

- **Secure Remove** is a secure file-deletion utility that ensures sensitive data is **permanently unrecoverable** using modern cryptographic techniques. 
- Instead of simply deleting a file, this tool **encrypts**, **overwrites**, **renames**, and **wipes metadata**, making forensic recovery extremely difficult—even with advanced tools.
